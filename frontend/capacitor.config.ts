import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pinn.b2bvarejista',
  appName: 'PINN B2B Varejista',
  webDir: 'dist'
};

export default config;
